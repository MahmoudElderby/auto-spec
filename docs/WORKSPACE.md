# Workspace layout

**Canonical repo path:** `C:\work\ai\auto-spec` (this repository).

Historical clutter (removed):

| Path | Was |
|------|-----|
| `auto-spec-github` | Temporary clone used to push the first GitHub release; merged into `auto-spec`. |
| `auto-spec-test-cursor` / `auto-spec-test-codex` | Disposable Spec Kit smoke-test projects. |
| `auto-spec-test-project` | npm install smoke test. |

To smoke-test installs, use a new empty folder outside this repo, e.g. `C:\work\ai\_scratch\my-spec-kit-test`.
